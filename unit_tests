const { loginUser } = require('../src/frontend/login');

test('Login function should return true when valid credentials are provided', () => {
    const result = loginUser('testuser', 'password123');
    expect(result).toBe(true);
});
